<!doctype html>
<html lang="mk">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>data aggregation Dashboard v2 â€” Ð¡ÐºÐ¾Ð¿Ñ˜Ðµ (Ð¼Ð°Ð³Ð°Ñ†Ð¸Ð½Ð¸/Ð¿Ð»Ð°Ñ†Ð¾Ð²Ð¸)</title>

  <!-- Ð›ÐµÑÐµÐ½, Ñ‡Ð¸Ñ‚Ð»Ð¸Ð² ÑÑ‚Ð¸Ð» (Ð¼Ð¾Ð¶ÐµÑˆ Ð´Ð° Ð³Ð¾ ÑÐ¼ÐµÐ½Ð¸Ñˆ Ð¿Ð¾ Ð¶ÐµÐ»Ð±Ð°) -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/simpledotcss/simple.min.css">

  <!-- DataTables v2 (Ð±ÐµÐ· jQuery Ð·Ð°Ð²Ð¸ÑÐ½Ð¾ÑÑ‚) -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/datatables.net-dt@2.1.8/css/dataTables.dataTables.min.css">
  <style>
    :root { --card-bg: #fff; --card-br: 12px; --card-shadow: 0 1px 6px rgba(0,0,0,.07); }
    .grid { display: grid; gap: 1.2rem; }
    .grid-2 { grid-template-columns: 1fr 1fr; }
    @media (max-width: 920px) { .grid-2 { grid-template-columns: 1fr; } }
    .card { background: var(--card-bg); border-radius: var(--card-br); padding: 1rem; box-shadow: var(--card-shadow); }
    .nowrap { white-space: nowrap; }
    table.dataTable tbody tr:hover { background: #f7f7ff; }
    header h1 { margin-bottom: .25rem; }
    header p { margin-top: .25rem; color: #555; }
  </style>
</head>
<body>
  <header>
    <h1>Ð¡ÐºÐ¾Ð¿Ñ˜Ðµ â€” ÐœÐ°Ð³Ð°Ñ†Ð¸Ð½Ð¸ &amp; ÐŸÐ»Ð°Ñ†Ð¾Ð²Ð¸ (HTML Dashboard v2)</h1>
    <p>Ð“ÐµÐ½ÐµÑ€Ð¸Ñ€Ð°Ð½Ð¾: {{ generated_at }}</p>
  </header>

  <section class="grid grid-2">
    <div class="card" id="chart_dist">
      {{ plot_dist|safe }}
    </div>
    <div class="card" id="chart_scatter">
      {{ plot_scatter|safe }}
    </div>
    <div class="card" id="chart_top_munis">
      {{ plot_top_munis|safe }}
    </div>
    <div class="card" id="chart_volume">
      {{ plot_volume|safe }}
    </div>
  </section>

  <section class="card">
    <h2>Top Ð¿Ð¾Ð½ÑƒÐ´Ð¸ (Ð¿Ñ€Ð²Ð¸Ñ‚Ðµ 100 Ð¿Ð¾ ÑÐºÐ¾Ñ€/ÑÐ²ÐµÐ¶Ð¸Ð½Ð°)</h2>
    {% if rows|length == 0 %}
      <p><em>ÐÐµÐ¼Ð° Ð¿Ð¾Ð´Ð°Ñ‚Ð¾Ñ†Ð¸ Ð·Ð° Ñ‚Ð°Ð±ÐµÐ»Ð°. ÐŸÑƒÑˆÑ‚Ð¸ crawl, Ð¿Ð° report.</em></p>
    {% else %}
      <p>Ð¡Ð¾Ð²ÐµÑ‚: ÐºÐ»Ð¸Ðº Ð½Ð° Ð·Ð°Ð³Ð»Ð°Ð²Ð¸Ðµ ÑÐ¾Ñ€Ñ‚Ð¸Ñ€Ð°; â€žÐŸÑ€ÐµÐ±Ð°Ñ€Ð°Ñ˜â€œ Ñ„Ð¸Ð»Ñ‚Ñ€Ð¸Ñ€Ð°; Ñ‚Ð°Ð±ÐµÐ»Ð°Ñ‚Ð° Ðµ responsive.</p>
      <table id="tbl" class="display nowrap" style="width:100%">
        <thead>
          <tr>
            {% for c in columns %}
              <th>{{ c }}</th>
            {% endfor %}
          </tr>
        </thead>
        <tbody>
          {% for r in rows %}
            <tr>
            {% for c in columns %}
              {% if c == "url" %}
                <td><a href="{{ r[c] }}" target="_blank" rel="noopener">Ð»Ð¸Ð½Ðº</a></td>
              {% else %}
                <td>{{ r[c] }}</td>
              {% endif %}
            {% endfor %}
            </tr>
          {% endfor %}
        </tbody>
      </table>
    {% endif %}
  </section>

  <!-- DataTables 2 (vanilla) -->
  <script src="https://cdn.jsdelivr.net/npm/datatables.net@2.1.8/js/dataTables.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const tbl = document.getElementById('tbl');
      if (tbl) {
        new DataTable(tbl, {
          paging: true,
          pageLength: 25,
          fixedHeader: true,
          responsive: true
        });
      }
    });
  </script>
</body>
</html>



